Imports System

Module Program
    Sub Main(args As String())
        Dim num1 As Integer = 100
        Dim num2 As Integer = 75
        Dim total As Long
        total = num1 + num2
        Console.WriteLine(total)
    End Sub
End Module
